тег url в шаблонах - Позволяет обратиться к html-шаблону по имени. {% url '<name шаблона>' %}

работая с данными из базы данных (посты например), лучше всего создать функцию, которая будет
строить адресную ссылку в самом файле моделей models.py в классе модели (Women).
Делать это следует через django.urls.reverse, например вот так:

def get_absolute_path(request):
    return reverse("<префикс>", kwargs={"post_id": self.pk})

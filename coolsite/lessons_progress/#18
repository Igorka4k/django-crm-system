Работа с пагинатором в python console

p = Paginator(women, 3) - women - Список элементов, которые необходимо отображать,
3 - Кол-во элементов, которые необходимо отображать на одной странице сайта.

У пагинатора "p" есть много различных свойств, которые описаны в доках

Так как во встроенном классе представления ListView уже имеется пагинация,
то в дочернем классе WomenHome достаточно указать лишь параметры (paginate_by)
Параметры page_obj и paginator передаются в шаблон указанный в template_name по-умолчанию

При использовании функций представления следует инициализировать пагинатор самостоятельно:
w_list = Women.objects.all()
paginator = Paginator(w_list, 3)
page_number = request.GET("page")
page_obj = paginator.get_page(page_number)

Номер страницы указывается в GET запросе (добавить ?page=<номер> в адресной ссылке)

не забываем в models.py у Women включить закоменченную сортировку (ordering), так как по этому поводу
при пагинации может возникнуть неудобное предупреждение в консоли.
 Настроим её по id (необязательно)
